name: Update Subscription Files Hourly

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-files:
    runs-on: ubuntu-latest
    steps:
      # 1. 检出仓库代码
      - name: Checkout repository
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0

      # 2. 下载所有订阅内容 (1个原有 + 5个新增)
      - name: Download subscription files
        run: |
          echo "Starting download of all subscription files..."
          
          # --- 下载原有的文件 ---
          curl -Ls "https://www.x9527.xyz/e258977b-e413-4718-a3af-02d75492c349/sub" -o cf-b64.txt
          echo "✓ cf-b64.txt downloaded"
          
          # --- 下载新增的5个文件 ---
          
          # 下载 Clash 格式
          curl -Ls "https://all.xmm1993.top/sub?token=123&clash" -o clash-stash.txt
          echo "✓ clash-stash.txt downloaded"
          
          # 下载 Surge 格式
          curl -Ls "https://www.x9527.xyz/e258977b-e413-4718-a3af-02d75492c349/sub?target=surge" -o surge.txt
          echo "✓ surge.txt downloaded"
          
          # 下载 Sing-box 格式
          curl -Ls "https://www.x9527.xyz/e258977b-e413-4718-a3af-02d75492c349/sub?target=singbox" -o singbox.txt
          echo "✓ singbox.txt downloaded"
          
          # 下载 Loon 格式
          curl -Ls "https://www.x9527.xyz/e258977b-e413-4718-a3af-02d75492c349/sub?target=loon" -o loon.txt
          echo "✓ loon.txt downloaded"
          
          # 下载 Quantumult X 格式
          curl -Ls "https://www.x9527.xyz/e258977b-e413-4718-a3af-02d75492c349/sub?target=quanx" -o quanx.txt
          echo "✓ quanx.txt downloaded"
          
          echo "All files downloaded successfully!"

      # 3. 配置Git用户信息
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # 4. 提交并推送所有更改 (包含全部6个文件)
      - name: Commit and push changes
        run: |
          # ✅ 添加所有文件：原有的1个 + 新增的5个
          git add cf-b64.txt clash-stash.txt surge.txt singbox.txt loon.txt quanx.txt
          
          # 检查暂存区是否有变化
          if ! git diff --cached --quiet; then
            git commit -m "Automated update: Hourly subscription files update [skip ci]"
            git push
            echo "✅ All 6 subscription files updated and pushed successfully."
          else
            echo "ℹ️ No changes detected in any subscription files. Nothing to commit."
          fi
